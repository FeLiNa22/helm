{{- if eq .Values.postgresql.mode "cluster" }}
{{- if .Values.postgresql.cluster.secret.create }}
{{- $secretName := .Values.postgresql.cluster.secret.name | default (printf "%s-immich-db-app" .Release.Name) }}
{{- $existingSecret := lookup "v1" "Secret" .Release.Namespace $secretName }}
{{- $password := "" }}
{{- if $existingSecret }}
{{- $password = index $existingSecret.data "password" | b64dec }}
{{- else }}
{{- $password = .Values.postgresql.cluster.secret.password | default (randAlphaNum 32) }}
{{- end }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "immich.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
type: kubernetes.io/basic-auth
stringData:
  username: {{ .Values.postgresql.cluster.secret.username }}
  password: {{ $password }}
{{- end }}

{{- if .Values.postgresql.cluster.superuserSecret.create }}
{{- $superuserSecretName := .Values.postgresql.cluster.superuserSecret.name | default (printf "%s-immich-db-superuser" .Release.Name) }}
{{- $existingSuperuserSecret := lookup "v1" "Secret" .Release.Namespace $superuserSecretName }}
{{- $superuserPassword := "" }}
{{- if $existingSuperuserSecret }}
{{- $superuserPassword = index $existingSuperuserSecret.data "password" | b64dec }}
{{- else }}
{{- $superuserPassword = .Values.postgresql.cluster.superuserSecret.password | default (randAlphaNum 32) }}
{{- end }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $superuserSecretName }}
  labels:
    {{- include "immich.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
type: kubernetes.io/basic-auth
stringData:
  username: {{ .Values.postgresql.cluster.superuserSecret.username }}
  password: {{ $superuserPassword }}
{{- end }}
{{- end }}
