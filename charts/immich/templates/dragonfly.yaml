{{- $fullName := include "immich.fullname" . -}}
{{- $secretName := include "immich.dragonfly.secretName" . -}}

{{- if and (or (eq .Values.dragonfly.mode "standalone") (eq .Values.dragonfly.mode "cluster")) .Values.dragonfly.auth.enabled }}
{{- if .Values.dragonfly.auth.password.value }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "immich.labels" . | nindent 4 }}
    app.kubernetes.io/component: dragonfly
type: kubernetes.io/basic-auth
stringData:
  username: {{ include "immich.dragonfly.username" . }}
  password: {{ .Values.dragonfly.auth.password.value }}
{{- else if not .Values.dragonfly.auth.password.secretName }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "immich.labels" . | nindent 4 }}
    app.kubernetes.io/component: dragonfly
type: kubernetes.io/basic-auth
stringData:
  username: {{ include "immich.dragonfly.username" . }}
  password: {{ randAlphaNum 32 }}
{{- end }}
{{- end }}

{{- if eq .Values.dragonfly.mode "standalone" }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $fullName }}-dragonfly
  labels:
    {{- include "immich.labels" . | nindent 4 }}
    app.kubernetes.io/component: dragonfly
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "immich.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: dragonfly
  template:
    metadata:
      labels:
        {{- include "immich.labels" . | nindent 8 }}
        app.kubernetes.io/component: dragonfly
    spec:
      containers:
        - name: dragonfly
          image: "{{ .Values.dragonfly.standalone.image.repository }}:{{ .Values.dragonfly.standalone.image.tag }}"
          command: 
            - "dragonfly"
            - "--default_lua_flags=allow-undeclared-keys"
            {{- if .Values.dragonfly.auth.enabled }}
            - "--requirepass=$(DRAGONFLY_PASSWORD)"
            {{- end }}
          imagePullPolicy: IfNotPresent
          {{- if .Values.dragonfly.auth.enabled }}
          env:
            - name: DRAGONFLY_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "immich.dragonfly.secretName" . }}
                  key: {{ include "immich.dragonfly.passwordKey" . }}
          {{- end }}
          ports:
            - name: redis
              containerPort: 6379
              protocol: TCP
          {{- if .Values.dragonfly.standalone.persistence.enabled }}
          volumeMounts:
            - name: data
              mountPath: /data
          {{- end }}
          livenessProbe:
            tcpSocket:
              port: redis
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: redis
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            {{- toYaml .Values.dragonfly.standalone.resources | nindent 12 }}
      {{- if .Values.dragonfly.standalone.persistence.enabled }}
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ $fullName }}-dragonfly
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $fullName }}-dragonfly
  labels:
    {{- include "immich.labels" . | nindent 4 }}
    app.kubernetes.io/component: dragonfly
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "immich.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: dragonfly
{{- if .Values.dragonfly.standalone.persistence.enabled }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $fullName }}-dragonfly
  labels:
    {{- include "immich.labels" . | nindent 4 }}
    app.kubernetes.io/component: dragonfly
spec:
  accessModes:
    - {{ .Values.dragonfly.standalone.persistence.accessMode | quote }}
  {{- if .Values.dragonfly.standalone.persistence.storageClass }}
  storageClassName: {{ .Values.dragonfly.standalone.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.dragonfly.standalone.persistence.size }}
{{- end }}
{{- end }}

{{- if eq .Values.dragonfly.mode "cluster" }}
---
apiVersion: dragonflydb.io/v1alpha1
kind: Dragonfly
metadata:
  name: {{ $fullName }}-dragonfly-cluster
  labels:
    {{- include "immich.labels" . | nindent 4 }}
    app.kubernetes.io/component: dragonfly-cluster
spec:
  args:
    - "--default_lua_flags=allow-undeclared-keys"
  {{- if .Values.dragonfly.auth.enabled }}
  authentication:
    passwordFromSecret:
      name: {{ include "immich.dragonfly.secretName" . }}
      key: {{ include "immich.dragonfly.passwordKey" . }}
  {{- end }}
  replicas: {{ .Values.dragonfly.cluster.replicas }}
  image: "{{ .Values.dragonfly.cluster.image.repository }}:{{ .Values.dragonfly.cluster.image.tag }}"
  {{- if .Values.dragonfly.cluster.resources }}
  resources:
    {{- toYaml .Values.dragonfly.cluster.resources | nindent 4 }}
  {{- end }}
  {{- if .Values.dragonfly.cluster.persistence.enabled }}
  snapshot:
    cron: {{ .Values.dragonfly.cluster.snapshot.cron | quote }}
    persistentVolumeClaimSpec:
      accessModes:
        - {{ .Values.dragonfly.cluster.persistence.accessMode | quote }}
      {{- if .Values.dragonfly.cluster.persistence.storageClass }}
      storageClassName: {{ .Values.dragonfly.cluster.persistence.storageClass }}
      {{- end }}
      resources:
        requests:
          storage: {{ .Values.dragonfly.cluster.persistence.size }}
  {{- end }}
  {{- with .Values.nodeSelector }}
  nodeSelector:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.tolerations }}
  tolerations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.affinity }}
  affinity:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
