{{- if .Values.acestream.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-acestream
  labels:
    app.kubernetes.io/name: acestream
    app.kubernetes.io/instance: {{ .Release.Name }}-acestream
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
    app.kubernetes.io/part-of: {{ .Release.Name | quote }}
spec:
  type: {{ .Values.acestream.service.type | default "ClusterIP" }}
  ports:
    # HTTP port (always enabled)
    - port: {{ .Values.acestream.service.http.port | default 6878 }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if (and (eq .Values.acestream.service.type "NodePort") (not (empty .Values.acestream.service.http.nodePort))) }}
      nodePort: {{ .Values.acestream.service.http.nodePort }}
      {{- end }}
    # UDP port (always enabled)
    - port: {{ .Values.acestream.service.udp.port | default 8621 }}
      targetPort: udp
      protocol: UDP
      name: udp
      {{- if (and (eq .Values.acestream.service.type "NodePort") (not (empty .Values.acestream.service.udp.nodePort))) }}
      nodePort: {{ .Values.acestream.service.udp.nodePort }}
      {{- end }}
    # Dispatcharr port (always enabled)
    - port: {{ .Values.acestream.service.dispatcharr.port | default 9191 }}
      targetPort: dispatcharr
      protocol: TCP
      name: dispatcharr
      {{- if (and (eq .Values.acestream.service.type "NodePort") (not (empty .Values.acestream.service.dispatcharr.nodePort))) }}
      nodePort: {{ .Values.acestream.service.dispatcharr.nodePort }}
      {{- end }}
    {{- if .Values.acestream.stepdaddylivehd.enabled }}
    # StepDaddyLiveHD port
    - port: {{ .Values.acestream.service.stepdaddylivehd.port | default 3000 }}
      targetPort: stepdaddy
      protocol: TCP
      name: stepdaddy
      {{- if (and (eq .Values.acestream.service.type "NodePort") (not (empty .Values.acestream.service.stepdaddylivehd.nodePort))) }}
      nodePort: {{ .Values.acestream.service.stepdaddylivehd.nodePort }}
      {{- end }}
    {{- end }}
    {{- if and .Values.acestream.gluetun.enabled .Values.acestream.gluetun.httpProxy.enabled }}
    # Gluetun HTTP proxy port
    - port: {{ .Values.acestream.gluetun.httpProxy.port | default 8888 }}
      targetPort: http-proxy
      protocol: TCP
      name: http-proxy
    {{- end }}
    {{- if and .Values.acestream.gluetun.enabled .Values.acestream.gluetun.shadowsocksProxy.enabled }}
    # Gluetun Shadowsocks TCP port
    - port: {{ .Values.acestream.gluetun.shadowsocksProxy.port | default 8388 }}
      targetPort: shadowsocks-tcp
      protocol: TCP
      name: shadowsocks-tcp
    # Gluetun Shadowsocks UDP port
    - port: {{ .Values.acestream.gluetun.shadowsocksProxy.port | default 8388 }}
      targetPort: shadowsocks-udp
      protocol: UDP
      name: shadowsocks-udp
    {{- end }}
  selector:
    app.kubernetes.io/name: acestream
    app.kubernetes.io/instance: {{ .Release.Name }}-acestream
{{- end }}
