{{- if and .Values.radarr.enabled (or (eq .Values.radarr.database.mode "external") (eq .Values.radarr.database.mode "cluster")) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-radarr-config
  labels:
    app.kubernetes.io/name: radarr
    app.kubernetes.io/instance: {{ .Release.Name }}-radarr
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
    app.kubernetes.io/part-of: {{ .Release.Name | quote }}
data:
  db-config.xml: |
    {{- if eq .Values.radarr.database.mode "cluster" }}
    <PostgresUser>{{ .Values.radarr.database.auth.username | default "radarr" }}</PostgresUser>
    <PostgresPassword>__DB_PASSWORD__</PostgresPassword>
    <PostgresHost>{{ .Values.radarr.database.cluster.name | default (printf "%s-radarr-db" .Release.Name) }}-rw</PostgresHost>
    <PostgresPort>{{ .Values.radarr.database.external.port }}</PostgresPort>
    {{- else }}
    <PostgresUser>{{ required "radarr.database.auth.username is required when radarr.database.mode is 'external'" .Values.radarr.database.auth.username }}</PostgresUser>
    <PostgresPassword>__DB_PASSWORD__</PostgresPassword>
    <PostgresHost>{{ required "radarr.database.external.host is required when radarr.database.mode is 'external'" .Values.radarr.database.external.host }}</PostgresHost>
    <PostgresPort>{{ .Values.radarr.database.external.port }}</PostgresPort>
    {{- end }}
    <PostgresMainDb>{{ .Values.radarr.database.external.mainDatabase }}</PostgresMainDb>
    <PostgresLogDb>{{ .Values.radarr.database.external.logDatabase }}</PostgresLogDb>
{{- end }}
