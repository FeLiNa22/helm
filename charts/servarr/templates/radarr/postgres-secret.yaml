{{- if and .Values.radarr.enabled (eq .Values.radarr.database.mode "cluster") .Values.radarr.database.auth.password (not .Values.radarr.database.auth.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-radarr-db-secret
  labels:
    app.kubernetes.io/name: radarr-db
    app.kubernetes.io/instance: {{ .Release.Name }}-radarr-db
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
    app.kubernetes.io/part-of: {{ .Release.Name | quote }}
type: kubernetes.io/basic-auth
stringData:
  username: {{ .Values.radarr.database.auth.username | default "radarr" }}
  password: {{ .Values.radarr.database.auth.password }}
{{- end }}
