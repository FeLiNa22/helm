{{- if and .Values.sonarr.enabled (eq .Values.sonarr.database.mode "cluster") .Values.sonarr.database.auth.password (not .Values.sonarr.database.auth.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-sonarr-db-secret
  labels:
    app.kubernetes.io/name: sonarr-db
    app.kubernetes.io/instance: {{ .Release.Name }}-sonarr-db
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
    app.kubernetes.io/part-of: {{ .Release.Name | quote }}
type: kubernetes.io/basic-auth
stringData:
  username: {{ .Values.sonarr.database.auth.username | default "sonarr" }}
  password: {{ .Values.sonarr.database.auth.password }}
{{- end }}
